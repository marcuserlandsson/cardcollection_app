# Logo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the placeholder favicon and generic nav icon with a custom CardBoard logo — two crossed trading cards with "CB" lettering.

**Architecture:** Single SVG asset used in two places: as the favicon (`public/favicon.svg`) and as an inline React component in the nav bar. No build tooling changes needed.

**Tech Stack:** SVG, Next.js App Router, React, TypeScript

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `public/favicon.svg` | Replace | Browser favicon — standalone SVG file |
| `components/icons/cardboard-logo.tsx` | Create | Reusable inline SVG React component for the logo |
| `components/nav/top-nav-bar.tsx` | Modify (line 7, 42) | Swap LayoutGrid import/usage for CardBoardLogo |

Note: `app/layout.tsx` already references `/favicon.svg` with the correct type — no changes needed there.

---

### Task 1: Create the favicon SVG

**Files:**
- Replace: `public/favicon.svg`

- [ ] **Step 1: Replace favicon.svg with the logo**

Replace the contents of `public/favicon.svg` with:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 110 110" fill="none">
  <g transform="rotate(12, 62, 55)">
    <rect x="30" y="14" width="52" height="70" rx="7" fill="#1e2230" stroke="#2dd4a8" stroke-width="2.5" opacity="0.45"/>
  </g>
  <g transform="rotate(-6, 42, 55)">
    <rect x="18" y="16" width="52" height="70" rx="7" fill="#1e2230" stroke="#2dd4a8" stroke-width="2.5"/>
    <rect x="24" y="22" width="40" height="58" rx="4" fill="none" stroke="#2dd4a8" stroke-width="1.2" opacity="0.25"/>
    <text x="44" y="58" font-family="system-ui, -apple-system, sans-serif" font-size="28" font-weight="900" text-anchor="middle">
      <tspan fill="#ffffff">C</tspan><tspan fill="#2dd4a8">B</tspan>
    </text>
  </g>
</svg>
```

- [ ] **Step 2: Verify favicon renders**

Run: `npm run dev`

Open `http://localhost:3000` in the browser. Check the browser tab — the favicon should show two crossed cards with "CB". Also open `http://localhost:3000/favicon.svg` directly to inspect the raw SVG.

- [ ] **Step 3: Commit**

```bash
git add public/favicon.svg
git commit -m "feat: replace placeholder favicon with CardBoard logo"
```

---

### Task 2: Create the logo React component

**Files:**
- Create: `components/icons/cardboard-logo.tsx`

- [ ] **Step 1: Create the component**

Create `components/icons/cardboard-logo.tsx`:

```tsx
interface CardBoardLogoProps {
  size?: number;
  className?: string;
}

export default function CardBoardLogo({ size = 20, className }: CardBoardLogoProps) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 110 110"
      fill="none"
      width={size}
      height={size}
      className={className}
    >
      <g transform="rotate(12, 62, 55)">
        <rect x="30" y="14" width="52" height="70" rx="7" fill="#1e2230" stroke="#2dd4a8" strokeWidth="2.5" opacity="0.45" />
      </g>
      <g transform="rotate(-6, 42, 55)">
        <rect x="18" y="16" width="52" height="70" rx="7" fill="#1e2230" stroke="#2dd4a8" strokeWidth="2.5" />
        <rect x="24" y="22" width="40" height="58" rx="4" fill="none" stroke="#2dd4a8" strokeWidth="1.2" opacity="0.25" />
        <text x="44" y="58" fontFamily="system-ui, -apple-system, sans-serif" fontSize="28" fontWeight="900" textAnchor="middle">
          <tspan fill="#ffffff">C</tspan>
          <tspan fill="#2dd4a8">B</tspan>
        </text>
      </g>
    </svg>
  );
}
```

Note: SVG attributes use React camelCase (`strokeWidth`, `fontFamily`, `fontSize`, `fontWeight`, `textAnchor`).

- [ ] **Step 2: Verify it compiles**

Run: `npm run build`
Expected: No TypeScript errors.

- [ ] **Step 3: Commit**

```bash
git add components/icons/cardboard-logo.tsx
git commit -m "feat: add CardBoardLogo React component"
```

---

### Task 3: Replace LayoutGrid in the nav bar

**Files:**
- Modify: `components/nav/top-nav-bar.tsx:7,42`

- [ ] **Step 1: Update the import**

In `components/nav/top-nav-bar.tsx`, change line 7 from:

```tsx
import { Home, Search, Layers, SquareStack, TrendingUp, LogOut, LogIn, LayoutGrid } from "lucide-react";
```

to:

```tsx
import { Home, Search, Layers, SquareStack, TrendingUp, LogOut, LogIn } from "lucide-react";
import CardBoardLogo from "@/components/icons/cardboard-logo";
```

- [ ] **Step 2: Replace the icon usage**

In `components/nav/top-nav-bar.tsx`, change line 42 from:

```tsx
          <LayoutGrid size={20} className="text-[var(--accent)]" />
```

to:

```tsx
          <CardBoardLogo size={20} />
```

No `className` needed — the colors are baked into the SVG.

- [ ] **Step 3: Verify in the browser**

Run: `npm run dev`

Open `http://localhost:3000`. The top nav bar should show the crossed-cards logo next to "CardBoard" text. Verify the logo is sized correctly (~20px) and aligns with the text.

- [ ] **Step 4: Run the build**

Run: `npm run build`
Expected: No errors. The unused `LayoutGrid` import is removed, so no lint warnings.

- [ ] **Step 5: Commit**

```bash
git add components/nav/top-nav-bar.tsx
git commit -m "feat: use CardBoard logo in nav bar, replacing LayoutGrid icon"
```
